#pragma once
#include <symbols/graphics/devices.h>
#include <stl/function.h>

namespace bora {
class bnWindow;
}   

class bnGraphics {
// public:
//     ResourceHandle<ITexture>* CreateTextureCommand(const TextureDesc& d, const void* data = nullptr, ResourceHandle<ITexture>* resource = nullptr);
//     void Submit();
// private:
//     bora::stl::vector<IGPUCommand> commands;
//     CommandBuffer buffer;
};